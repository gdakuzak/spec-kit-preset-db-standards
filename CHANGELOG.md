# Changelog

## 0.1.0 — unreleased

- Initial preset.
- `spec-template`: appends a "Data Requirements" section (incl. external input that reaches the database).
- `plan-template`: appends a "Database Design" section (naming conventions, OLTP/OLAP classification, normalization — 3NF for OLTP — and denormalization trade-offs, principle-based OLAP guidance, schema changes, SQL portability / ANSI-first, query safety / SQL injection, indexing plan, N+1 / query volume).
- `tasks-template`: appends a "Schema Review Checklist" (naming, integrity, normalization for OLTP & OLAP, SQL portability, query safety / SQL injection, performance, migration safety, data handling).
